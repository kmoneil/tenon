# Plans, scratch, reviews. Never committed.
_plan/
_tmp/
_reviews/
_reports/
# The benchmark's staging directory.
_bench/

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Go workspace file
go.work
go.work.sum

# AI
.claude
CLAUDE.md

# IDE
.vscode/

# Local make settings, conformance coverage records and the determinism
# harness's outputs (see Makefile)
local.mk
.rulecov/
.emit/

# Commands built in the module root
/rulecheck
